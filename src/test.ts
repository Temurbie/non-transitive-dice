import promptSync from 'prompt-sync';
const prompt = promptSync();

const userInput = prompt("Pick a number between 0 and 100: ");
const userNum = parseInt(userInput);
let prom = prompt("sdsdsd")
let r = parseInt(prom)
console.log(`You picked: ${userNum}`);
console.log(r);

